Entity:
   get_images
   get_image
   get_name
   next_image
   entity_string

Background(Entity):
   pass

Positionable(Entity):
   set_position
   get_position

Obstacle(Positionable):
   create_from_properties

Actor(Positionable):
   remove_pending_action
   add_pending_action
   get_pending_actions
   clear_pending_actions
   create_action
   schedule

Ore(Actor):
   get_rate
   create_transform_action

Vein(Actor):
   get_rate
   get_resource_distance
   create_from_properties

Blacksmith(Actor):
   set_resource_count
   get_resource_count
   get_resource_limit
   get_resource_distance

Quake(Actor):
   get_animation_rate
   create_animation_action

Mover(Quake):
   next_position
   to_*

OreBlob(Mover):
   transform

Miner(Mover, Blacksmith):
   pass